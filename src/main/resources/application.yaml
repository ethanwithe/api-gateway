server:
  port: ${PORT:8080}

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      # Configuración Global de CORS
      globalcors:
        add-to-simple-url-handler-mapping: true
        cors-configurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:3000"
              - "https://tu-frontend.railway.app"  # ⬅️ Agregar tu dominio de Railway si tienes frontend
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - PATCH
              - OPTIONS
            allowedHeaders: "*"
            exposedHeaders: "*"
            allowCredentials: true
            maxAge: 3600

      # Rutas del Gateway
      routes:
        # Servicio Usuario
        - id: servicio-usuario
          uri: https://servicio-usuario-production.up.railway.app
          predicates:
            - Path=/api/users/**

        # Servicio RRHH
        - id: servicio-rrhh
          uri: servicio-rrhh-production.up.railway.app
          predicates:
            - Path=/api/rrhh/**

        # Servicio Inventario
        - id: servicio-inventario
          uri: https://servicio-inventario-production-5a12.up.railway.app
          predicates:
            - Path=/api/inventario/**

        # Servicio Customer
        - id: servicio-customer
          uri: https://serviciocustomer-production.up.railway.app
          predicates:
            - Path=/api/clientes/**

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka-server.railway.internal:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${random.value}  # ⬅️ CAMBIO: Usar random en vez de puerto fijo
    hostname: ${RAILWAY_PUBLIC_DOMAIN:localhost}  # ⬅️ NUEVO: Hostname dinámico

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,gateway

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    org.springframework.cloud.loadbalancer: DEBUG